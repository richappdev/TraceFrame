/** Remote Config boolean on the Server template. */
export const SITE_ACCESS_BLOCKED_KEY = "site_access_blocked";

/** Short TTL so Console toggles reach open sessions within a few seconds. */
export const SITE_ACCESS_CACHE_TTL_MS = 10_000;
