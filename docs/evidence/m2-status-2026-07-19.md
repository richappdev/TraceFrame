# M2 implementation status — 2026-07-19

> **Historical pre-rename snapshot.** Preserve the 46-test/build result and legacy host references below as evidence from this date. Current active docs use the AniPins brand, the `antiable-anipin` host, and the 31-ID registry; a post-fix hosted locale recheck is still required as tracked evidence.

## Automated preflight

- `npm run test`: **46 passing** across 12 files.
- `npm run typecheck`: pass for all workspaces.
- `npm run lint`: pass.
- `npm run build`: pass.
- Python verifier: compile pass; offline canonical seed build returns 14 candidates.

## Corrections included after review

- Historical Anitabi verifier is bounded and sequential, uses a fixed identifying client, and stops the whole run on 403, 429, or HTML challenge responses.
- Trip create/edit enforce the actual 64 KiB streamed body limit and reject malformed or unsupported inputs with 4xx errors.
- Production instrumentation validates storage, OAuth, callback, and session configuration at runtime startup.
- `/api/health` checks Firestore readability and returns 503 when runtime dependencies are not ready.
- Firebase Hosting `web.app` is the canonical OAuth origin; alternate hosts redirect OAuth start there.

## Remaining gate

M2 remains **OPEN**. The canonical Firebase Hosting deployment still requires manual evidence for OAuth, owner isolation, persistence across rollout/restart, share rotation/revocation, logged-out sharing, deletion, accessibility, and recovery/rollback review. Historical raw smoke reports are not release evidence because they contain personal data and recorded gaps.

Unauthenticated hosted smoke on 2026-07-19 (`docs/evidence/hosted-smoke-2026-07-19.md`) confirmed health, locale routes, Presence, and Explore on `https://antiable-traceframe.web.app`, and found locale preference broken behind Hosting (`traceframe_locale` / now `anipins_locale` stripped; `Accept-Language` rewritten). Fix is in middleware/`i18n` and needs redeploy before re-checking. Use [`docs/hosted-smoke.md`](../hosted-smoke.md) for the operator checklist; it does not replace [`docs/m2-acceptance.md`](../m2-acceptance.md).
