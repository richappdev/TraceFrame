# tools/

Utility packages for Anitabi research. Each tool lives in its own subfolder.

| Folder | Purpose |
|---|---|
| [`probe-agent/`](./probe-agent/) | API `/lite` probe agent (EN + CHT docs) |

Add new tools as `tools/<tool-name>/` with their own README.
